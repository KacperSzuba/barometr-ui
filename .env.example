# Where the Kotlin backend is. Deliberately not NEXT_PUBLIC_: from here on the browser
# never reaches it. Every call goes through this application's own route handlers, which
# is what lets the access token live in an HttpOnly cookie the browser cannot read — and
# why the backend has CORS switched off.
BAROMETR_API_URL=http://localhost:8080
